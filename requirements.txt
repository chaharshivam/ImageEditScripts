# Core (images). Pins are floors plus a ceiling so a future major bump cannot
# silently break Python 3.9.
Pillow>=9.1,<12
numpy>=1.21,<3

# Web app (webapp.py / Framewipe.command)
Flask>=2.2,<4

# Video features additionally need ffmpeg on PATH:  brew install ffmpeg
# Optional extras (not installed by default): see requirements-optional.txt
# Dev extras (pytest): see requirements-dev.txt
